{
  "name": "desktop",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "dev:renderer": "npm --prefix renderer run dev -- --host 127.0.0.1 --port 5173",
    "dev": "set ELECTRON_RENDERER_URL=http://127.0.0.1:5173&& electron .",
    "build:renderer": "npm --prefix renderer run build",
    "start": "npm run build:renderer && electron ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "electron": "^42.1.0"
  }
}
